{
  "name": "api-server-all",
  "version": "1.0.0",
  "description": "API server for DB connection check and table list",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "build:frontend": "npm run build --prefix frontend"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mysql2": "^3.6.5"
  }
}
